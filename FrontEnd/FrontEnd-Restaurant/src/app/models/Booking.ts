import { Time } from "@angular/common";

export class Booking{
    id!: number;
    name!: string;
    lastName!: string;
    phoneNumber!: number;
    numberOfPeople!: number;
    date!: Date;
    time!: string;
}